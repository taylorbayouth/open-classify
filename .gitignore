node_modules/
dist/
.env
*.local
.DS_Store
.claude/
open-classify.log
adapter-models.json

# Training data is user-supplied; only guides and evals ship in the repo.
training/training-data/*.jsonl

# Draft evals require hand-labeling before they're ready to commit.
training/evals/draft/

# Trained adapter weights land here after fine-tuning. Files are large and
# user-specific; keep the folder structure visible via *.md only.
training/adapters/*
!training/adapters/*.md
